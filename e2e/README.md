# E2E tests with RobotFramework

## Setup

### Python env init

Make Python env and install dependencies:

`python -m venv venv`

`pip install -r requirements.txt`

## Run tests (example)

`robot --outputdir test_outputs tests/`
